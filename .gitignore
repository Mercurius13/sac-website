# dependencies & build output (Render rebuilds these)
node_modules/
dist/

# secrets - never commit (repo is public)
.env
.env.*

# local admin uploads (production commits these to the repo via the GitHub API)
public/media/uploads/

# os / editor cruft
.DS_Store

# curl cookie jars from local testing
$JAR
cookies.txt

# local-only dev preview server
preview-server.js
